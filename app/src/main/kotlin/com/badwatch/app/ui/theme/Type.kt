package com.badwatch.app.ui.theme

import androidx.wear.compose.material3.Typography

/**
 * Type follows the Wear Material 3 scale, which is already tuned for round displays —
 * including the `numeral*` family used for the big HUD readouts and the `arc*` family
 * used by curved text along the bezel. Defaults are kept on purpose; screens should
 * reach for `numeralExtraLarge`/`numeralLarge` for glanceable numbers instead of
 * inventing ad-hoc sizes.
 */
val BadWatchTypography = Typography()
